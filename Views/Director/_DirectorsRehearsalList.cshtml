@model IEnumerable<TomorrowsVoice_Toplevel.Models.Rehearsal>
<br />
<div class="panel panel-primary">
	<div class="panel-heading">
		<h4 class="panel-title">List of Rehearsals</h4>
	</div>
	<div class="panel-body scrollable-panel">
		<table class="table table-striped">
			<thead>
				<tr>
					<th aria-label="Rehearsal">
						Rehearsal
					</th>
					<th aria-label="Note">
						Note
					</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var item in Model.OrderByDescending(r => r.RehearsalDate))
				{
					<tr>
						<td>
							<a asp-controller="Rehearsal" asp-action="Details"
							   asp-route-id="@item.ID" asp-route-directorID="@ViewBag.DirectorID">@Html.DisplayFor(modelItem => item.Summary)</a>
						</td>
						<td>
							@Html.DisplayFor(modelItem => item.Note)
						</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>